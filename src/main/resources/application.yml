spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ED_api_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  application:
    name: ED-api  
